@model IEnumerable<AgenciaDeViagens_MVC_Dot_Net.Models.Promocao>

@{
    ViewData["Title"] = "Index";
}

<link rel="stylesheet" href="~/css/Destino.css" asp-append-version="true" />
<!-- INÍCIO DO BANNER -->
        <div class="position-relative overflow-hidden md-1 md-1">
            <img src="../../IMG/Promocao.jpg" style="height: 700px; width: 100%;">
        </div>
<!-- FIM DO BANNER -->
<h1>Listagem de Destinos</h1>
<p>
    <a asp-action="Create" class="btn btn-primary">Cadastrar Novo Destino</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LocalViagem)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descricao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ValorViagem)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LocalViagem)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descricao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ValorViagem)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PromocaoId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.PromocaoId">Detalhar</a> |
                <a asp-action="Delete" asp-route-id="@item.PromocaoId">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
